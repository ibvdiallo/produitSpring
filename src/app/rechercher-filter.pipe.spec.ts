import { RechercherFilterPipe } from './rechercher-filter.pipe';

describe('RechercherFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RechercherFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
